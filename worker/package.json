{
  "name": "bot-tiendas",
  "version": "1.0.0",
  "private": true,
  "description": "Bot de ventas por Instagram (Cloudflare Worker). El mismo codigo atiende a varias tiendas; cual, lo dice TIENDA en wrangler.toml.",
  "scripts": {
    "login": "wrangler login",
    "quien-soy": "wrangler whoami",
    "probar": "wrangler deploy --dry-run",
    "desplegar": "wrangler deploy",
    "ver-logs": "wrangler tail",
    "local": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.138.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
